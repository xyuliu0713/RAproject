import os
import os.path as op

import dipy
import matplotlib.pyplot as plt
import nibabel as nib

from AFQ.api.group import GroupAFQ
import bids

from dipy.data import fetch_bundle_atlas_hcp842

fetch_bundle_atlas_hcp842()

bids_path = '/Volumes/xyu/ds006169-1'

my_afq = GroupAFQ(
    bids_path,
    preproc_pipeline='mrtrix',
    import_tract={
        "extension": "tck",
        "scope": "tractography"
    },
)

# 🔥 1️⃣ 生成 bundles（tract segmentation）

bundles = my_afq.export("bundles")

print("Bundles done:", bundles)

# 🔥 2️⃣ 生成 tract profiles（FA沿束）

profiles = my_afq.export("profiles")

print("Profiles done:", profiles)

# 🔥 3️⃣ 生成可视化（3D tract 图）

viz = my_afq.export("viz")

print("Visualization done:", viz)

#my_afq.export_all()
#print(my_afq.export())

