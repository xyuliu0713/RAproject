from bids import BIDSLayout

import os

bids_path = '/Volumes/xyu/ds006169-3'

from bids import BIDSLayout

layout = BIDSLayout(bids_path, derivatives=True)

files = layout.get(scope='tractography', suffix='tractography')

print(files)